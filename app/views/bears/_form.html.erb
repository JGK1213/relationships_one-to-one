<%= form_for(@bear) do |f| %>
  <% if @bear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bear.errors.count, "error") %> prohibited this bear from being saved:</h2>

      <ul>
      <% @bear.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
