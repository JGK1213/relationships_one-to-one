<%= form_for(@river) do |f| %>
  <% if @river.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@river.errors.count, "error") %> prohibited this river from being saved:</h2>

      <ul>
      <% @river.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
   <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
